# Porta do servidor
# Render e Railway definem automaticamente a PORT em produção
PORT=3001

# Ambiente (development | production)
NODE_ENV=development

# CORS Origin - URL do cliente (opcional, usa * por padrão)
# Em produção, defina para a URL do seu cliente para maior segurança
# Exemplo: https://oraculo-client.onrender.com
# CORS_ORIGIN=*
